# Optional site URL for metadata and social sharing.
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Optional OpenAI config for run-scoped RAG on /runs/[id].
# Leave unset to keep the assistant in retrieval-only mode.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.4-mini
